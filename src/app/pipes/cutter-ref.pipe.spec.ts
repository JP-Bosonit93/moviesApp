import { CutterRefPipe } from './cutter-ref.pipe';

describe('CutterRefPipe', () => {
  it('create an instance', () => {
    const pipe = new CutterRefPipe();
    expect(pipe).toBeTruthy();
  });
});
